<%= form_with(model: comment, url: url, local: true, method:method) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :com %>
    <%= form.text_area :com %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
